# Load the project at the start of every interactive session
# For windows machines, you may need to set the working directory here too with
# set.wd("/absolute/path/to/project")
library(ProjectTemplate)
load.project()
